---
const { items = [] } = Astro.props
---
<section class="py-16 sm:py-20">
	<div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
		<h2 class="text-3xl font-bold tracking-tight text-zinc-900 dark:text-zinc-50 sm:text-4xl">Testimonianze</h2>
		<div class="mt-10 grid grid-cols-1 gap-6 md:grid-cols-3">
			{(items.length ? items : [
				{ author: 'CTO, PMI', quote: 'Impatto immediato su delivery e qualità.' },
				{ author: 'Marketing Lead', quote: 'Chatbot efficaci e on-brand.' },
				{ author: 'IT Manager', quote: 'Intranet M365 adottata dal 100% del team.' },
			]).map((t) => (
				<blockquote class="rounded-2xl shadow-sm ring-1 ring-black/5 bg-white dark:bg-zinc-900 p-6">
					<p class="text-zinc-900 dark:text-zinc-50 mb-3">“{t.quote}”</p>
					<cite class="text-zinc-700 dark:text-zinc-300">{t.author}</cite>
				</blockquote>
			))}
		</div>
	</div>
</section>
