---
const { title = 'Claim principale', subtitle = 'Sottotitolo breve', primaryHref = '#', primaryLabel = 'Call to action', secondaryHref = '#', secondaryLabel = 'Scopri di più' } = Astro.props
---
<section class="py-24">
	<div class="container">
		<h1 class="text-6xl mb-4">{title}</h1>
		<p class="text-2xl mb-8">{subtitle}</p>
		<div class="flex gap-4">
			<a class="button" href={primaryHref}>{primaryLabel}</a>
			<a class="button button-variant-ghost" href={secondaryHref}>{secondaryLabel}</a>
		</div>
	</div>
</section>
